<nav class="navbar navbar-expand-lg navbar-dark">
    <div class=" container-fluid">
        <img id="logo" src="assets/images/logo2.jpg" alt="logo" />
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="navbar-brand text-ternary" href="/">Accueil</a>
                <a class="nav-link active text-secondary" aria-current="page" href="#">Florilège</a>
                <div class="collapse navbar-collapse" id="navbarNavDarkDropdown">
                    <ul class="navbar-nav">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                Mon compte
                            </a>
                            <ul class="dropdown-menu text-secondary" aria-labelledby="navbarDarkDropdownMenuLink">

                                <li class="offset-1"> {% if (session.user) %}
                                   <div class="text-primary">Bonjour {{ session.user.name }} ! </div></li>
                                <li><a class="dropdown-item text-secondary" href="/profil?id={{ session.user.id }}">Profil</a></li>
                                <li><a class="dropdown-item text-secondary" href="/logout">Déconnexion</a></li>
                                {% else %} </li>
                                <li><a class="dropdown-item text-secondary" href="/connection">Connexion</a></li>
                                <li><a class="dropdown-item text-secondary" href="/registration">Inscription</a></li>
                                {% endif %}
                                {% if(session.user.is_admin ) %}
                                <li><a class="dropdown-item text-secondary" href="#">Admin</a></li>
                                {% endif %}

                             </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
</nav>
<div class="container">
    <div class="row">
        <span class="col border border-secondary"></span>
    </div>
</div>
